# 当前理想化副本最新构建状态

本文件记录最后一次构建后的状态，避免把早期诊断运行误认为最终验收。

- 控制器 MEX：`main_5mw_idealized.mexw64`，已重新编译成功；
- VSG：严格 VSG 分支、物理相对角约定、`VSG_POWER_ERROR_SIGN=+1`；
- 控制时间步：4 us 调用；原离散 PI 积分系数已按连续化时间步缩放；
- GSC 电压环：已加入网侧电流前馈，前馈符号当前取 `+1`；
- SVPWM：理想连续编译模式下不再调用 `svpwm1.calc/svpwm2.calc`，仅保留接口兼容代码；
- 原模型：未修改。

最近一次完整短时诊断（在最终绕过 SVPWM 调用之前）仍显示 `Udc` 和 `Ppcc` 没有闭合到 5 MW 稳态，故当前副本仍属于“理想化调试版本”，不属于论文验收版本。下一步必须迁移 MEX 内部状态为显式连续状态，并从同一平衡点重新初始化功率面和 DC-link 能量。
